<% if super_authorized? %>
	<%= link_to( image_tag("fail.png", :size => "16x16"), @list, { :title => "Delete", :class => 'ajax-delete'}) %>
	<%= link_to( 'delete list', @list, { :title => "Delete", :class => 'ajax-delete'}) %>
	<br />
<% end %>
<% if authorized? %>
	<%= link_to( image_tag("pencil.png", :size => "16x16"), edit_list_path(@list), {:title => "Edit", :class => 'modal update_page_section_' + @page_section.id.to_s }) %>
	<%= link_to( 'edit list', edit_list_path(@list), {:title => "Edit", :class => 'modal update_page_section_' + @page_section.id.to_s }) %>
	<br />
	<%= link_to image_tag("add.png", :size => "16x16"), new_list_list_item_path(@list), :class => 'modal update_page_section_' + @page_section.id.to_s %>
	<%= link_to "Add item", new_list_list_item_path(@list), :class => 'modal update_page_section_' + @page_section.id.to_s %>
<% end %>
<% content_tag_for(:ul, @list) do %>
	<%= render :partial => 'list_items/show', :collection => @list.list_items, :as => :list_item %>
<% end %>